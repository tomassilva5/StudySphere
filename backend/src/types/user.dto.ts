export default interface CreateUserDTO {
    username:string;
    fullname:string;
    email:string;
    password:string;
}
export default interface UserDTO{
    username:string;
    password:string;
}